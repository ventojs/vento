{
  "tasks": {
    "test": "deno test --allow-read test",
    "bench": "deno bench --allow-read",
    "docs": "cd docs && deno task serve",
    "build": "deno run --allow-run=npm --allow-env --allow-read --allow-write --allow-net=deno.land scripts/build_npm.ts"
  },
  "lock": false,
  "fmt": {
    "exclude": ["CHANGELOG.md", "npm", "docs"]
  }
}
